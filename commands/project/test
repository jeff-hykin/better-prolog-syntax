#!/usr/bin/env bash

commands/project/build && npx textmate-tester \
    --textmateExtension prolog \
    --syntax "$FORNIX_FOLDER/autogenerated/prolog.tmLanguage.json" \
    --examples "$FORNIX_FOLDER/language_examples/" \
    generate-specs --all